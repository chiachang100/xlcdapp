import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.joyolord.app.xlcdapp',
  appName: 'xlcdapp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
